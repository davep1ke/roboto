# Legacy WinForms/.NET Framework project - not part of the new build, and dragging its checked-in
# binary DLL + packages into the Docker build context just slows builds down.
Roboto/
Roboto.sln

# Test project - dev-time only, the Dockerfile never builds or needs it.
tests/

.git/
.vs/
**/bin/
**/obj/
**/.vscode/

data/
*.md
